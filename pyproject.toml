[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["F601", "F821", "E722"]

[project]
name = "secuscan"
version = "0.1.0"
dependencies = []

[project.scripts]
secuscan = "backend.secuscan.cli:main"
